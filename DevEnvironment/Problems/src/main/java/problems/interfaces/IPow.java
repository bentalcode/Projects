package problems.interfaces;

/**
 * The IPow interface defines a pow function for comuting a pow of a number.
 */
public interface IPow {
    /**
     * Comutes the pow of a number.
     */
    double pow(double value, int pow);
}
