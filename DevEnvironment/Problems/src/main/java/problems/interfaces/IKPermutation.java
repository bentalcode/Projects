package problems.interfaces;

/**
 * The IKPermutation interface defines a k permutation.
 */
public interface IKPermutation {
    /**
     * Gets the K permutation of an integer array in it's sorted order
     */
    int[] kPermutation(int size, int k);
}
