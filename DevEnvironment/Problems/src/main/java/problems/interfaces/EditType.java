package problems.interfaces;

/**
 * The EditType enum defines a type of an edit, a change or correction made as a result of an editing.
 */
public enum EditType {
    Insert,
    Remove,
    Replace
}
